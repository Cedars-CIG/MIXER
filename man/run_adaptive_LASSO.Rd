% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step3_adalasso.R
\name{run_adaptive_LASSO}
\alias{run_adaptive_LASSO}
\title{Run adaptive LASSO with data-driven lambda range tuning}
\usage{
run_adaptive_LASSO(
  y_train,
  feature_train,
  df_weight,
  min_num = 10,
  max_prop = 0.05,
  lambda_init_min = 150,
  lambda_init_max = 200,
  nlambda = 100,
  max_iter = 50,
  family = "binomial"
)
}
\arguments{
\item{y_train}{Training outcome vector.}

\item{feature_train}{Training feature matrix/data.frame (rows = samples, cols = features).}

\item{df_weight}{Data.frame with columns \code{SNP} and \code{weight}.}

\item{min_num}{Target minimum number of selected features when tuning \code{lambda_max}.}

\item{max_prop}{Target maximum proportion of selected features when tuning \code{lambda_min};
\code{max_num = max_prop * nrow(df_weight)}.}

\item{lambda_init_min}{Initial guess for \code{lambda_min}.}

\item{lambda_init_max}{Initial guess for \code{lambda_max}.}

\item{nlambda}{Number of lambdas in the final cross-validated grid.}

\item{max_iter}{Maximum number of iterations when searching \code{lambda_min} and \code{lambda_max}.}

\item{family}{glmnet family (default: \code{"binomial"}).}
}
\value{
A data.frame with columns \code{SNP} and \code{beta} for nonzero adaptive LASSO coefficients.
}
\description{
This function tunes \code{lambda_min} and \code{lambda_max} to target a desired
range of selected features, then performs cross-validated adaptive LASSO using
\code{glmnet} with \code{penalty.factor} determined by \code{df_weight}.
}
